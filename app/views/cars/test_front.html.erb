<!--   <h1 class="h1-results"><%#= Car.count %> cars in Barcelona</h1> -->
  <!-- a -->


<!-- <div class="web-container"> -->
  <h1 class="h1-results"><%= Car.count %> cars in Barcelona</h1>
  <div class="container">
    <div class="row justify-content-center">
      <div class="cars-list">
       <% @cars.each do |car|  %>
       <div class="col-xs-12 div col-md-4">
          <div class="car-card">
            <%= link_to car_path(car) do %>
            <%= cl_image_tag car.photo, class: "card-photo"  %>
              <div class="car-price">
                <p><%= car.brand %>  <%= car.model %></p>
                <p>€ <%= car.price %> / day</p>
              </div>
            <% end %>
          </div>
      <!-- </div> -->
      </div>
      <% end %>
    </div>
  </div>
</div>

